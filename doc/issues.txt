Doesn't work for music files at the root folder
  path looks something like /path/to/file//filename

Always assumes the file is a jpg
  was workaround because eyed3 just had to know the mimetype

Always clears any existing album art
  may not really be an issue
